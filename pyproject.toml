[project]
name = "image-matching-test"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "opencv-python-headless>=4.8",
    "matplotlib>=3.7",
]

[tool.uv]
package = false
